import { ReactElement } from 'react';

import Button from '../../../src/components/Button';

const App = (): ReactElement => (
  <div>
    Hello world!<Button>111</Button>
  </div>
);

export default App;
